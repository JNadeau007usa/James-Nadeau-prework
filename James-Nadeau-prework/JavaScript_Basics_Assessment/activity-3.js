//Start off by creating an array with three student names.
const studentList= ['Jim', 'Jeff', 'Mike'];
//Create a loop that will prompt the user for three more names.
var name = 'input'
while(studentList= ['Jim', 'Jeff', 'Mike']);
const name4= prompt('add a name')
const name5= prompt('add a name')
const name6= prompt('add a name')
//After every user input, store the new name into the array.
studentList.push('name4');
studentList.push('name5');
studentList.push('name6');
//Create a new loop that will iterate through the array and console log each element of the array.
for(let i = 0; i < studentList.length; i++) {
    const student = studenList[i];
    console.log(student);
}